// Phase 5 で実装（jsPDF + html-to-image）。Phase 0 では占位のみ。
export default function ExportPdfButton() {
  return (
    <button
      disabled
      title="Phase 5 で実装"
      className="font-jp text-[12px] border border-line rounded-full px-3 py-1 text-ink-soft/60"
    >
      PDF出力（準備中）
    </button>
  );
}
