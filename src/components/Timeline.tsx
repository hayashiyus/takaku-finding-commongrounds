// Phase 5 で実装（seq/created_at 昇順に出現を再生・スクラブ）。Phase 0 では占位のみ。
export default function Timeline() {
  return (
    <button
      disabled
      title="Phase 5 で実装"
      className="font-jp text-[12px] border border-line rounded-full px-3 py-1 text-ink-soft/60"
    >
      ▶ 再生（準備中）
    </button>
  );
}
